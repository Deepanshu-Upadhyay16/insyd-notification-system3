# Simplified Railway Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy backend files
COPY backend/ ./

# Install dependencies
RUN npm install

# Build TypeScript
RUN npm run build

# Expose port
EXPOSE 5000

# Start the application
CMD ["npm", "start"]
